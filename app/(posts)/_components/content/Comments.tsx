import React from "react";
import { StyleSheet, View } from "react-native";

const Comments = () => {
  return <View style={styles.container}>Comments area</View>;
};

export default Comments;

const styles = StyleSheet.create({
  container: {},
});
